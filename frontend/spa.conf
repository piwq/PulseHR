# Внутренний nginx фронта: отдаёт SPA, history-fallback на index.html.
server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
